import { test, expect } from '@playwright/test';


test('Select form', async ({ page }) => {
  await page.goto('https://playwright.dev/');


});

test('Fill the content', async ({ page }) => {
  await page.goto('https://playwright.dev/');
});


